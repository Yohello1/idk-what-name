// blah blah blah
class shader
{
    shader(const char *verFtile, const char* fragFile)
    {
        std::string vertCode = get_file_contents(vertFile);
        std::string fragCode = get_file_contents(fragFile);
    }
};
